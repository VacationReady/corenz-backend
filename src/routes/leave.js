// src/routes/leave.js
const express = require('express');
const router = express.Router();

// Placeholder GET endpoint
router.get('/', (req, res) => {
  res.send('Leave route placeholder');
});

module.exports = router;
